// Track-role colours are assigned at creation and never inherited. Audio is
// also enforced at render so every project shows the same track colour.
// Audio sits on a solid blue near the accent hue (#35a7e6); the waveform is
// a lighter tint of the same hue so the peaks read as ink on the surface.
export const OBJECT_TRACK_COLOR = '#3a7694'
export const MOVER_TRACK_COLOR = '#62589a'
export const COLORIZER_TRACK_COLOR = '#8a4f73'
export const AUDIO_TRACK_COLOR = '#1f4e86'
export const AUDIO_WAVEFORM_COLOR = '#7fc0ee'
